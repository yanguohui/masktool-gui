# ===== 打包工具 =====
pyinstaller>=6.0
packaging

# ===== 脱敏核心 mask-tool =====
# 该库尚未发布到 PyPI，需从 GitHub 安装。
# 已固定到本地验证通过的提交，保证 Actions 构建结果一致。
# 安装后 PyInstaller 会把它整体打包进 exe（见 masktool_gui.spec），
# 最终用户无需单独安装 mask-tool。
mask-tool @ git+https://github.com/ZagooYWX/mask-tool.git@e39f04765e7f5ab705826f41d60003629f3cea8a

# ===== 可选：spaCy NER 后端 =====
# 仅当你在 GUI 的「识别引擎」里选 spaCy / 自动 并想用 spaCy 识别时才需要。
# 打包后的单文件程序默认不含 spaCy（体积 / 许可考虑），会自动回退 jieba。
# 源码 / 开发运行如需启用 spaCy：
#   pip install spacy
#   python -m spacy download zh_core_web_sm
spacy
